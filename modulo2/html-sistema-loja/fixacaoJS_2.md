```
function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  let total = 0
  if (quantidade<12) {
    total = quantidade * 1.3
  } else {
    total = quantidade
  }
  return total
}
```